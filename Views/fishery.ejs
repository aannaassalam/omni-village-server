<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OmniVillage Fisheries List</title>
    <style>
      body {
        padding: 2rem;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        color: #333;
      }

      h2,
      h3 {
        text-align: center;
      }

      body small {
        display: block;
      }

      body > div {
        padding: 1rem;
        margin-bottom: 15px;
      }

      body > div > div {
        border-bottom: 1px dashed #ccc;
      }

      body > div * {
        margin-bottom: 8px;
      }

      body > div h4 {
        text-decoration: underline;
        margin-bottom: 25px;
      }

      body > div > div > div {
        margin-bottom: 40px;
      }

      body > div > div > div strong {
        color: #444;
        font-weight: 500;
      }
    </style>
  </head>
  <body>
    <%- include("./partials/header");%>
    <h2>OmniVillage Fisheries List</h2>
    <h3>Sorted by latest to oldest</h3>
    <div>
      <% Object.keys(fisheries).forEach((item)=>{%>
      <div>
        <h4><%=item %></h4>
        <% fisheries[item].forEach((fishery)=>{%>
        <div>
          <small>ID: <%=fishery._id%></small>
          <strong
            ><%=fishery.user.first_name%> <%=fishery.user.last_name%></strong
          >
          <p><%=fishery.user.country_code%> <%=fishery.user.phone%></p>
          <p>fishery Type: <%=fishery.fishery_type%></p>
          <p>
            fishery Crop: <%=fishery?.fishery_crop ? fishery?.fishery_crop?.name
            : "CROP DELETED" %>
          </p>
        </div>
        <%});%>
      </div>
      <%});%>
    </div>
  </body>
</html>
